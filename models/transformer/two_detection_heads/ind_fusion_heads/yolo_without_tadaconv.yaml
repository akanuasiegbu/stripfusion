# NJOTWANI

# parameters
nc: 1  # number of classes
depth_multiple: 1.00  # model depth multiple
width_multiple: 1.00  # layer channel multiple

# anchors
anchors:
  - [10,13, 16,30, 33,23]   # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

backbone:
  [
    # stream one
    [-1, 1, Focus, [64, 3]],    # 0-P1/2
    [-1, 1, Conv, [128, 3, 2]], # 1-P2/4
    [-1, 3, C3, [128]],   # 2-P2/4
    [-1, 1, Conv, [256, 3, 2]],  # 3-P3/8
    [-1, 9, C3, [256]],   # 4-P3/8
    # stream two
    [-4, 1, Focus, [64, 3]],    # 5-P1/2
    [-1, 1, Conv, [128, 3, 2]], # 6-P2/4
    [-1, 3, C3, [128]],   # 7-P2/4
    [-1, 1, Conv, [256, 3, 2]],  # 8-P3/8
    [-1, 9, C3, [256]],   # 9-P3/8
    
    # Keep the TransformerBlock and fusion layers as they are
    [[4,9], 1, StripMLPTemporalMixerv2, [256, 80, 800, 2]],   # 10-P3/8
    [[4,10], 1, Add2, [256,0]],    # 11-P3/8 stream one x+trans[0]
    [[9,10], 1, Add2, [256,1]],    # 12-P3/8 stream two x+trans[1]

    # Continue with the rest of the backbone, changing Conv3d to Conv2d
    [11, 1, Conv, [512, 3, 2]],   # 13-P4/16
    [-1, 9, C3, [512]],   # 14-P4/16
    [12, 1, Conv, [512, 3, 2]],   # 15-P4/16
    [-1, 9, C3, [512]],   # 16-P4/16
    
    # Keep the fusion layers
    [[14,16], 1, StripMLPTemporalMixerv2, [512, 40, 200, 2]],   # 17-P3/8
    [[14,17], 1, Add2, [512,0]],    # 18-P3/8 stream one x+trans[0]
    [[16,17], 1, Add2, [512,1]],    # 19-P3/8 stream two x+trans[1]

    # Continue with the rest of the backbone, changing Conv3d to Conv2d
    [18, 1, Conv, [1024, 3, 2]],    # 20-P5/32
    [-1, 1, SPP, [1024, [5, 9, 13]]],   # 21-P5/32
    [-1, 3, C3, [1024, False]],   # 22-P5/32
    [19, 1, Conv, [1024, 3, 2]],    # 23-P5/32
    [-1, 1, SPP, [1024, [5, 9, 13]]],   # 24-P5/32
    [-1, 3, C3, [1024, False]],   # 25-P5/32
    
    # Keep the fusion layers
    [[22,25], 1, StripMLPTemporalMixerv2, [1024, 20, 50, 2]],    # 26-P5/32
    [[22,26], 1, Add2, [1024,0]],    # 27-P5/32 stream one x+trans[0]
    [[25,26], 1, Add2, [1024,1]],    # 28-P5/32 stream two x+trans[1]

    # Keep the Add Block
    [[11,12], 1, Add, [1]],   # 29-P3/8 fusion backbone P3
    [[18,19], 1, Add, [1]],   # 30-P4/16 fusion backbone P4
    [[27,28], 1, Add, [1]],   # 31-P5/32 fusion backbone P5
  ]

headRGB:
  [
    [-1, 1, Conv, [512, 1, 1]],   # 32
    [-1, 1, nn.Upsample, [None, 2, 'nearest']],   # 33
    [[-1,30], 1, Concat, [1]],    # 34 cat backbone P4
    [-1, 3, C3, [512, False]],    # 35

    [-1, 1, Conv, [256, 1, 1]],   # 36
    [-1, 1, nn.Upsample, [None, 2, 'nearest']],   # 37
    [[-1,29], 1, Concat, [1]],    # 38 cat backbone P3
    [-1, 3, C3, [256, False]],    # 39 (P3/8-small)

    [-1, 1, Conv, [256, 3, 2]],   # 40
    [[-1,36], 1, Concat, [1]],    # 41 cat head P4
    [-1, 3, C3, [512, False]],    # 42 (P4/16-medium)

    [-1, 1, Conv, [512, 3, 2]],    # 43
    [[-1,32], 1, Concat, [1]],     # 44 cat head P5
    [-1, 3, C3, [1024, False]],     # 45 (P5/32-large)
  ]

headThermal:
  [
    [31,1, Conv, [512, 1, 1]],   # 46
    [-1, 1, nn.Upsample, [None, 2, 'nearest']],   # 47
    [[-1,30], 1, Concat, [1]],    # 48 cat backbone P4
    [-1, 3, C3, [512, False]],    # 49

    [-1, 1, Conv, [256, 1, 1]],  # 50
    [-1, 1, nn.Upsample, [None, 2, 'nearest']],   # 51
    [[-1,29], 1, Concat, [1]],    # 52 cat backbone P3
    [-1, 3, C3, [256, False]],    # 53 (P3/8-small)

    [-1, 1, Conv, [256, 3, 2]],    # 54
    [[-1,50], 1, Concat, [1]],    # 55 cat head P4
    [-1, 3, C3, [512, False]],    # 56 (P4/16-medium)

    [-1, 1, Conv, [512, 3, 2]],   # 57
    [[-1,46], 1, Concat, [1]],     # 58 cat head P5
    [-1, 3, C3, [1024, False]],     # 59 (P5/32-large)
  ]

head:
  [
    [[39,42,45, 53, 56, 59], 1, LastFrameThermalRgbDetectv2, [nc, anchors, [256, 512, 1024]]],   # 60 Detect(P3, P4, P5)
  ]

detectorIDs: [60]